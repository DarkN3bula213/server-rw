{
	"nodemonConfig": {
		"ignore": [
			"**/*.test.ts",
			"**/*.spec.ts",
			".git",
			"node_modules",
			"uploads"
		],
		"watch": ["src"],
		"exec": "node ./src/index.ts",
		"ext": "ts, js"
	},
	"scripts": {
		"dev": "nodemon"
	}
}
